export const metadata = {
  title: 'Free JWT Encoder - Create & Sign JWT Tokens Online | DevTools Hub',
  description: 'Create and sign JWT tokens online with custom payload and secret key. Support for HS256, HS384, HS512 algorithms. Free JWT encoder tool.',
  keywords: ['jwt encoder', 'create jwt', 'jwt generator', 'sign jwt', 'jwt token creator', 'json web token encoder'],
  openGraph: {
    title: 'Free JWT Encoder - Create JWT Tokens | DevTools Hub',
    description: 'Create and sign JWT tokens with custom payload and secret key.',
    type: 'website',
  },
}

export default function JWTEncoderLayout({ children }) {
  return children
}
