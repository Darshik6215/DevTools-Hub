export const metadata = {
  title: 'Free JWT Decoder & Debugger - Decode JSON Web Tokens Online | DevTools Hub',
  description: 'Decode and debug JWT tokens online. View header, payload, and signature. Verify JWT structure and inspect claims. Free JWT decoder tool.',
  keywords: ['jwt decoder', 'jwt debugger', 'decode jwt', 'json web token decoder', 'jwt parser', 'jwt inspector', 'jwt tool'],
  openGraph: {
    title: 'Free JWT Decoder & Debugger | DevTools Hub',
    description: 'Decode JWT tokens and inspect header, payload, and signature instantly.',
    type: 'website',
  },
}

export default function JWTDecoderLayout({ children }) {
  return children
}
