// SEO metadata for Password Generator page
export const metadata = {
  title: 'Password Generator',
  description: 'Generate secure random passwords with customizable options. Create strong passwords with uppercase, lowercase, numbers and symbols.',
}

export default function PasswordGeneratorLayout({ children }) {
  return children
}
